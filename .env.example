# Persistence mode: 'file' (default) or 'mysql'
# PERSISTENCE=mysql

# Production: MySQL connection for the running app (set in Railway env)
# DATABASE_URL=mysql://user:pass@host:port/database

# Local dev only: MySQL connection for "Publish to Railway" button
# RAILWAY_DB_URL=mysql://user:pass@host:port/database
